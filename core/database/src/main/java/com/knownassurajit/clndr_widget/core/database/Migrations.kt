package com.knownassurajit.clndr_widget.core.database

import androidx.room.migration.Migration

/**
 * v1 is the initial schema — no migrations yet.
 * Add hand-written [Migration] objects here whenever an @AutoMigration cannot cover the change.
 */
object Migrations {
    val ALL: Array<Migration> = emptyArray()
}
