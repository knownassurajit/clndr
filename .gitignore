# Gradle
.gradle/
build/
local.properties
gradle/**/build/
*.gradle.bak

# IntelliJ / Android Studio
*.iml
.idea/
*.iws
*.ipr
out/
captures/
.externalNativeBuild/
.cxx/
.settings/
.navigation/
*.hprof

# IDE & Editor
.vscode/
*.swp
*.swo
*~
*.sublime-workspace
.project

# OS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Android build outputs
*.apk
*.aab
*.ap_
*.aar
*.dex
*.class
*.so

# Signing (keep keystore.properties but ignore actual keystores)
*.keystore
*.jks
*.key

# Logs
*.log
hs_err_pid*

# KSP / Kotlin generated code (build/ & out/ already ignored above)
.kotlin/

# Native debug symbols (NDK debugSymbolLevel output lands in build/, but guard strays)
*.so.dbg
*.sym
native-debug-symbols.zip

# Web/Node dependencies (clndr project)
node_modules/
.npm
npm-debug.log*
yarn-error.log*
package-lock.json
yarn.lock

# Build cache
*.o
*.out

# References (top-level web prototype only — anchored so it doesn't match the com/clndr/ package path)
/clndr/